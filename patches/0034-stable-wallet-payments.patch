diff --git a/Projects/UOContent/Mobiles/Vendors/NPC/AnimalTrainer.cs b/Projects/UOContent/Mobiles/Vendors/NPC/AnimalTrainer.cs
index 6005884..aeb4662 100644
--- a/Projects/UOContent/Mobiles/Vendors/NPC/AnimalTrainer.cs
+++ b/Projects/UOContent/Mobiles/Vendors/NPC/AnimalTrainer.cs
@@ -187,8 +187,7 @@ public void BeginStable(Mobile from)
                 return;
             }
 
-            if (!(from.Backpack?.GetAmount(typeof(Gold)) >= 30) &&
-                !(Banker.GetBalance(from) >= 30))
+            if (!Server.UOOffline.HavenEconomy.CanPay(from, 30))
             {
                 SayTo(from, 1042556); // Thou dost not have enough gold, not even in thy bank account.
             }
@@ -237,7 +236,7 @@ public void EndStable(Mobile from, BaseCreature pet)
                     SayTo( from, 1048053 ); // You can't stable that!
                   }
             */
-            else if (pet is PackLlama or PackHorse or Beetle && pet.Backpack?.Items.Count > 0)
+            else if (pet is PackLlama or PackHorse or Beetle && Server.UOOffline.HavenEconomy.HasStableCargo(pet))
             {
                 SayTo(from, 1042563); // You need to unload your pet.
             }
@@ -251,7 +250,7 @@ public void EndStable(Mobile from, BaseCreature pet)
             }
             else
             {
-                if (from.Backpack?.ConsumeTotal(typeof(Gold), 30) == true || Banker.Withdraw(from, 30))
+                if (Server.UOOffline.HavenEconomy.TryPay(from, 30))
                 {
                     pet.ControlTarget = null;
                     pet.ControlOrder = OrderType.Stay;
