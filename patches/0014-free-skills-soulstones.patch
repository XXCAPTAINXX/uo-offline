diff --git a/Projects/UOContent/Items/Special/SoulStone.cs b/Projects/UOContent/Items/Special/SoulStone.cs
--- a/Projects/UOContent/Items/Special/SoulStone.cs
+++ b/Projects/UOContent/Items/Special/SoulStone.cs
@@ -331,7 +331,11 @@ public partial class SoulStone : Item, ISecurable
              * 11.6 free points. Also, if we're below our skillcap by, say, 8.2 points,
              * we only need 11.6 - 8.2 = 3.4 points.
              */
-            var requiredAmount = (int)(skillValue * 10) - fromSkill.BaseFixedPoint - (from.SkillsCap - from.SkillsTotal);
+            var freeSkill = Server.CustomBots.FreeSkillSystem.IsFree(from, fromSkill);
+            var requiredAmount = freeSkill
+                ? 0
+                : (int)(skillValue * 10) - fromSkill.BaseFixedPoint -
+                  (from.SkillsCap - Server.CustomBots.FreeSkillSystem.CappedTotalFixed(from));
 
             var cannotAbsorb = false;
 
@@ -349,7 +353,8 @@ public partial class SoulStone : Item, ISecurable
 
                 for (var i = 0; i < from.Skills.Length; ++i)
                 {
-                    if (from.Skills[i].Lock != SkillLock.Down)
+                    if (from.Skills[i].Lock != SkillLock.Down ||
+                        Server.CustomBots.FreeSkillSystem.IsFree(from, from.Skills[i]))
                     {
                         continue;
                     }
@@ -413,7 +418,8 @@ public partial class SoulStone : Item, ISecurable
             {
                 for (var i = 0; i < from.Skills.Length; ++i)
                 {
-                    if (from.Skills[i].Lock != SkillLock.Down)
+                    if (from.Skills[i].Lock != SkillLock.Down ||
+                        Server.CustomBots.FreeSkillSystem.IsFree(from, from.Skills[i]))
                     {
                         continue;
                     }
