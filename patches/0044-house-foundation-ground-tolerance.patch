diff --git a/Projects/UOContent/Multis/Houses/HousePlacement.cs b/Projects/UOContent/Multis/Houses/HousePlacement.cs
--- a/Projects/UOContent/Multis/Houses/HousePlacement.cs
+++ b/Projects/UOContent/Multis/Houses/HousePlacement.cs
@@ -168,7 +168,9 @@ namespace Server.Multis
                         }
                     }
 
-                    map.GetAverageZ(tileX, tileY, out var landStartZ, out var landAvgZ, out _);
+                    map.GetAverageZ(tileX, tileY, out var landStartZ, out var landAvgZ, out var landMaxZ);
+                    var supportsFoundation = Server.UOOffline.HavenHousing.SupportsFoundation(
+                        center.Z, landStartZ, landMaxZ, TileData.LandTable[landID].Flags);
 
                     var hasFoundation = false;
 
@@ -199,14 +201,12 @@ namespace Server.Multis
                             addTileTop += 16;
                         }
 
-                        if (addTileTop > landStartZ && landAvgZ > addTileZ)
+                        if (addTileTop > landStartZ && landAvgZ > addTileZ && !(isFoundation && supportsFoundation))
                         {
                             return HousePlacementResult.BadLand; // Broke rule #2
                         }
 
-                        if (isFoundation &&
-                            (TileData.LandTable[landTile.ID & TileData.MaxLandValue].Flags & TileFlag.Impassable) == 0 &&
-                            landAvgZ == center.Z)
+                        if (isFoundation && supportsFoundation)
                         {
                             hasSurface = true;
                         }
