# Despise central spawn area — September 13, 2026

Reduced only the named Felucca Despise champion base radius from 35 to 10 tiles around controller 5557,824. Native later-wave radius modifiers remain. SpawnArea is 20 by 20; confined roaming enabled. Existing uncontrolled wave enemies outside the bounds are relocated using native spawnable-location checks. The ten-second maintenance also returns strays; no kills or wave progress are reset. AutoRestart remains enabled.

Isolated build zero warnings/errors; all prior travel checks and 200 sampled spawn positions inside new bounds passed. Clean live save/shutdown, fresh Saves backup with hashes plus previous DLL/source under servuo-before-despise-radius-*. Live build zero warnings/errors, startup and login/relay probe passed. No client files changed. In-client combat appearance remains to be confirmed.
